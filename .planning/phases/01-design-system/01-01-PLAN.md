---
phase: 01-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/styles.css
  - apps/web/src/styles/tailwind.css
  - apps/web/src/styles/foundation.css
  - apps/web/src/styles/tailwind-components.css
  - apps/web/src/styles/console-theme-tokens.css
autonomous: true
requirements: [REQ-01, REQ-02, REQ-03, REQ-12]
user_setup: []

must_haves:
  truths:
    - "All color tokens in @theme inline use oklch() functional notation"
    - "Typography uses Inter (sans) and JetBrains Mono (mono) as the primary font stack"
    - "Three surface layers exist: --background, --surface, --surface-2"
    - "Base radius is --radius: 0.5rem with 1px low-chroma borders"
    - "Legacy foundation.css tokens still work (aliased behind oklch tokens)"
  artifacts:
    - path: "apps/web/src/styles/tailwind.css"
      provides: "@theme inline block with oklch tokens and font theme"
      contains: "oklch("
    - path: "apps/web/src/styles/foundation.css"
      provides: "Three surface layers, radius, border, typography variables"
      contains: "--surface"
    - path: "apps/web/src/styles.css"
      provides: "Google Fonts import for Inter and JetBrains Mono"
      contains: "fonts.googleapis.com"
  key_links:
    - from: "tailwind.css"
      to: "tailwind-components.css"
      via: "Tailwind @theme radius tokens consumed by @layer components"
      pattern: "var\\(--radius"
---

<objective>
Migrate the CSS design system foundation from the existing Octogent-vintage hex/color-mix tokens to the oklch-based semantic token system defined in `apps/design.md`. This establishes the color, typography, surface layering, and spacing/radius scale that all subsequent phases build on.

Purpose: The entire visual system depends on a coherent set of design tokens. This plan replaces raw hex values with oklch semantic tokens (REQ-01), establishes Inter + JetBrains Mono typography (REQ-02), creates the three-layer surface system (REQ-03), and sets the spacing/radius scale (REQ-12).

Output: Updated `tailwind.css`, `styles.css`, `foundation.css`, `tailwind-components.css`, and `console-theme-tokens.css` with oklch tokens and the new design foundation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/design.md
@apps/web/src/styles.css
@apps/web/src/styles/tailwind.css
@apps/web/src/styles/foundation.css
@apps/web/src/styles/tailwind-components.css
@apps/web/src/styles/console-theme-tokens.css

<interfaces>
<!-- Current CSS token mapping (from foundation.css → will be replaced/aliased):

Current (hex/color-mix):
  --bg-canvas: #020403
  --bg-surface-1: #07120a
  --bg-surface-2: #07120a
  --border-subtle: #1a2e05
  --text-primary: #f0f0f0
  --text-secondary: #d9f99d
  --text-muted: #84cc16
  --accent-primary: #a3e635
  --accent-secondary: #eab308
  --term-red: #880f1e
  --term-green: #33ff66
  --term-blue: #1e59a3
  --term-warning: #ffb020

Target (oklch from design.md):
  --background: oklch(0.16 ...)
  --surface: oklch(...)
  --surface-2: oklch(...)
  --foreground: near-white
  --muted-foreground: secondary text
  --border: low contrast hairline
  --brand/--primary: soft mint/lime accent
  --running: mint
  --stopped: warm orange
  --stale: amber
  --idle: neutral gray
  --destructive: delete/danger

Current Tailwind @theme:
  The @theme inline block maps --color-background → var(--bg-canvas) etc.
  This needs to change so semantic tokens use oklch directly.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Replace Tailwind @theme tokens with oklch semantic values</name>
  <files>
    apps/web/src/styles/tailwind.css
  </files>
  <read_first>
    apps/web/src/styles/tailwind.css,
    apps/design.md
  </read_first>
  <action>
    Rewrite the `@theme inline {}` block in `tailwind.css` to use oklch values instead of CSS variable references to foundation.css. The current block maps to `var(--bg-canvas)`, `var(--accent-primary)` etc. — replace ALL color values with direct oklch tokens.

    From the design.md spec, the semantic token mapping (using oklch approximation of the described colors — deep zinc canvas, soft mint accent, etc.):

    ```css
    @theme inline {
      --radius-sm: calc(var(--radius, 0.5rem) - 4px);
      --radius-md: calc(var(--radius, 0.5rem) - 2px);
      --radius-lg: var(--radius, 0.5rem);
      --radius-xl: calc(var(--radius, 0.5rem) + 4px);

      --color-background: oklch(0.16 0.01 145);
      --color-foreground: oklch(0.95 0.005 100);
      --color-surface: oklch(0.185 0.015 145);
      --color-surface-2: oklch(0.21 0.018 145);
      --color-card: oklch(0.185 0.015 145);
      --color-card-foreground: oklch(0.95 0.005 100);
      --color-popover: oklch(0.21 0.018 145);
      --color-popover-foreground: oklch(0.95 0.005 100);
      --color-primary: oklch(0.80 0.15 135);
      --color-primary-foreground: oklch(0.16 0.01 145);
      --color-secondary: oklch(0.21 0.018 145);
      --color-secondary-foreground: oklch(0.95 0.005 100);
      --color-muted: oklch(0.185 0.015 145);
      --color-muted-foreground: oklch(0.60 0.04 135);
      --color-accent: oklch(0.80 0.15 135);
      --color-accent-foreground: oklch(0.16 0.01 145);
      --color-destructive: oklch(0.50 0.18 25);
      --color-destructive-foreground: oklch(0.95 0.005 100);
      --color-border: oklch(0.25 0.01 145);
      --color-input: oklch(0.25 0.01 145);
      --color-ring: oklch(0.80 0.15 135);
      --color-info: oklch(0.55 0.15 260);

      --color-running: oklch(0.70 0.15 145);
      --color-stopped: oklch(0.70 0.15 55);
      --color-stale: oklch(0.70 0.15 85);
      --color-idle: oklch(0.55 0.05 260);
      --color-brand: oklch(0.80 0.15 135);

      --font-sans: "Inter", ui-sans-serif, system-ui, sans-serif;
      --font-mono: "JetBrains Mono", ui-monospace, monospace;
    }
    ```

    IMPORTANT: Keep the `@source ".."`, `@import "tw-animate-css"`, `@import "./tailwind-components.css"`, `@custom-variant dark` lines unchanged. Only replace the `@theme inline {}` block body.

    Keep the `.bg-grid`, `.scanline`, and `@keyframes pulse-ring` / `.animate-pulse-ring` rules at the bottom of the file — they are additive and will stay for now (Plan 01-02 will update them for oklch consistency if needed).
  </action>
  <verify>
    grep -c "oklch(" apps/web/src/styles/tailwind.css || echo "FAIL: no oklch found"
  </verify>
  <acceptance_criteria>
    - tailwind.css contains at least 15 `oklch(` tokens in the @theme block
    - tailwind.css contains `--font-sans: "Inter"`
    - tailwind.css contains `--font-mono: "JetBrains Mono"`
  </acceptance_criteria>
  <done>Tailwind @theme uses oklch values directly, font theme points to Inter and JetBrains Mono</done>
</task>

<task type="auto">
  <name>task 2: Add Google Fonts import and update foundation.css with surface layers</name>
  <files>
    apps/web/src/styles.css,
    apps/web/src/styles/foundation.css,
    apps/web/src/styles/tailwind-components.css,
    apps/web/src/styles/console-theme-tokens.css
  </files>
  <read_first>
    apps/web/src/styles.css,
    apps/web/src/styles/foundation.css,
    apps/web/src/styles/tailwind-components.css,
    apps/web/src/styles/console-theme-tokens.css,
    apps/design.md
  </read_first>
  <action>
    Make these changes across four files:

    **A. `apps/web/src/styles.css`:**
    Replace the existing Google Fonts `@import` URL. The current one imports Inter, JetBrains Mono, and Silkscreen. Replace with a clean import for Inter (400/500/600/700) and JetBrains Mono (400/500/600):

    ```css
    @import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap");
    ```

    (This is the same URL already there — if it already includes the right weights and families, leave it unchanged.)

    **B. `apps/web/src/styles/foundation.css`:**
    Add the three-layer surface system after the existing `:root` block. Do NOT remove existing variables (they may be consumed by legacy views) — add the new semantic tokens:

    ```css
    /* --- Design system surface layers (from apps/design.md) --- */
    --background: oklch(0.16 0.01 145);
    --surface: oklch(0.185 0.015 145);
    --surface-2: oklch(0.21 0.018 145);
    --foreground: oklch(0.95 0.005 100);
    --muted-foreground: oklch(0.60 0.04 135);
    --border: oklch(0.25 0.01 145);
    --brand: oklch(0.80 0.15 135);
    --radius: 0.5rem;
    ```

    Update the existing `--font-main` from "PP Neue Machina Plain" to point to Inter:
    ```css
    --font-main: "Inter", "JetBrains Mono", ui-monospace, monospace;
    ```

    Update `--ui-base-font-size`: Change from `18px` to `16px` (the design.md uses smaller base sizes — 13px body is the norm, 16px base with Tailwind's scale gets there).

    **C. `apps/web/src/styles/tailwind-components.css`:**
    No changes needed — the component layer already uses `var(--radius, 0.5rem)` which will pick up the new `--radius: 0.5rem` from foundation.css. Just verify.

    **D. `apps/web/src/styles/console-theme-tokens.css`:**
    Update any color values in this file to use the new semantic CSS variables instead of raw hex values. If the file references `#020403` or `#07120a` etc, replace those references with `var(--background)`, `var(--surface)`, etc. Do a grep for hex color patterns and replace each with its oklch variable counterpart if reasonably identifiable. If a hex doesn't have a direct semantic mapping, leave it (legacy view, will be migrated in later phases).
  </action>
  <verify>
    Write-Output "Verifying changes...";
    $pass = $true;
    if (-not (Select-String -Path "apps/web/src/styles/foundation.css" -Pattern "--background:" -Quiet)) { Write-Output "FAIL: --background not found"; $pass = $false };
    if (-not (Select-String -Path "apps/web/src/styles/foundation.css" -Pattern "--radius: 0.5rem" -Quiet)) { Write-Output "FAIL: --radius not found"; $pass = $false };
    if (-not (Select-String -Path "apps/web/src/styles/foundation.css" -Pattern '"Inter"' -Quiet)) { Write-Output "FAIL: Inter font not set"; $pass = $false };
    if ($pass) { Write-Output "PASS" } else { exit 1 }
  </verify>
  <acceptance_criteria>
    - foundation.css contains `--background:` with oklch value
    - foundation.css contains `--surface:` and `--surface-2:` with oklch values
    - foundation.css contains `--radius: 0.5rem`
    - foundation.css contains `"Inter"` in --font-main
    - foundation.css `--ui-base-font-size` is `16px`
  </acceptance_criteria>
  <done>Foundation CSS has oklch surface layers, Inter font, 0.5rem radius, 16px base font size</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS source → browser | CSS tokens are static and user-authored; no injection surface |
| Google Fonts CDN | External resource load — same as any web font; no sensitive data exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | Google Fonts URL | accept | Public Google Fonts CSS — no credentials, no PII, same-origin only |
| T-01-02 | T | CSS variable injection through content | accept | CSS variable values are statically written in source files; user-authored content is not interpolated into CSS variable values |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds (build verifies no broken CSS imports)
- [ ] `pnpm test` passes (no style-related test regressions)
- [ ] `grep -c "oklch(" apps/web/src/styles/tailwind.css` shows 15+ matches
- [ ] `grep -c "oklch(" apps/web/src/styles/foundation.css` shows 5+ matches
- [ ] `grep -- "--font-main" apps/web/src/styles/foundation.css` contains "Inter"
- [ ] `grep -- "--ui-base-font-size" apps/web/src/styles/foundation.css` shows "16px"
</verification>

<success_criteria>

- All oklch tokens defined in @theme inline of tailwind.css
- Inter and JetBrains Mono set as primary font stack
- Three surface layers (--background, --surface, --surface-2) defined in foundation.css
- --radius: 0.5rem and --border: oklch() established
- Build passes without errors
- Legacy foundation.css variables still functional (backward compatibility)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-01-SUMMARY.md`
</output>
