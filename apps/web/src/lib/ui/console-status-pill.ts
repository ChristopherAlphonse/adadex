import { cva } from "class-variance-authority";

export const consoleStatusPillVariants = cva(
  "inline-flex border border-primary/55 bg-primary/15 px-2 py-0.5 font-mono text-[0.76rem] font-semibold tracking-wide text-primary uppercase",
);
